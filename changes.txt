23.08.2026 17:56
Here is a big one.
UI Changes:
Move the theme selector into the Settings.
Revise the compare/diff engine to include some different most-used scenarios. For selected files only i.e. Now its just stuck on calculating.
Implement syntax highlighting to the editor, as well as search, search-and-replace.
A bit more subtle logout button - maybe under a profile button, possibility to tie pam/local login to built-in db-user, with own user profile if both are allowed. This might be a bigger job on the whole user-system. Since I also want to break out an own Administrator-settings/control panel, and be able to promote users pam/local or internal-db to admins that can do admin tasks on the admin-settings/control panel. Also be able to enable/disable services for users, deny all by default and enable cifs or nfs or s3 only for specified users. Again, this might need a more comprehensive user-system to be able to seamless integrate local/pam users to a internal-db user and let them have a profile, with email, profile photo, name/nick etc. 
Maybe this goes more under the implementation of oauth etc, then we do that ofc.
Show/hide dotfiles/hidden files should be a settings under settings-cog.
Eye can then be swapped for a favorites-button - right click opens favorites-manager.
Remove the "Quad-Pane" badge, its not that revolutionary.
Implementation of file-converters, imageconverters etc (convertx).
Compare, Editor, Sync/Syncthing/rclone/rsync etc can be put under a tools menu/button. Clean up this bar a bit.
Terminal - its own button.
